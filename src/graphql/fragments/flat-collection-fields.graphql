fragment FlatCollectionFields on FlatCollection {
  id
  originalID
  collectionType
  originalSlug
  title
  slug
  description
  whyCool
  whyCoolEn
  typicalStyle
  typicalStyleEn
  character
  characterEn
  iconKey
  wineryCode
  priceRange
  skin
  climate
  climateTemperature
  category
  colorGroup
  adjective { id title slug }
  flavour { id title slug category colorGroup }
  country { id title slug }
  climateData { id title slug }
  statistics { landArea wineriesCount }
  climateConditions { diurnalRange humidity }
  social { instagram website }
  synonyms { title }
  bestGrapes { id title slug }
  bestRegions { id title slug }
  legends { id title slug wineryCode }
  neighbours { id title slug }
  relatedWineries { id title slug wineryCode }
  distinctiveAromas { id title slug }
  blendingPartners { id title slug }
  similarVarieties { id title slug }
  tags { id title slug }
  media { id alt url thumbnailURL }
  seo { title description image }
  syncedAt
} 