// Simple pass-through middleware - locale is handled by page props
export function middleware(): void {
  // Do nothing - let Next.js handle routing manually
}

export const config = {
  matcher: [],
}
